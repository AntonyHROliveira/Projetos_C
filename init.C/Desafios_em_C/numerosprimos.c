#include<stdio.h>

int main(){
    int num_primo;

    printf("Verifique se um numero e primo.\n");
    printf("Digite um numero inteiro: ");
    scanf("%d", &num_primo);

    if((num_primo > 1) && (num_primo / 1 == num_primo)){
      if((num_primo / 2 == 0) && (num_primo ))
    }
    
    
    
    
    return 0;
}